# Week 03 章节修订说明

## 修订目标
将 QA 评分从 16/20 提升到 >= 18/20，重点改进趣味性和叙事吸引力。

## 修订内容

### 1. 第1节开头：增强冲击力 ✅
**原文问题**：开头比较平铺直叙，缺少代入感
**修订方案**：
- 从"接手别人的代码"这个真实场景切入
- 用"改第一处、改第二处、改第三处……"制造焦虑感
- 小北的反应更夸张："那我写的代码……以后也会变成别人的噩梦吗？"
- 老潘用亲身经历（500行函数，改了50多处还漏了2处）增强真实感

### 2. 增加"哦！"时刻 ✅

#### 时刻1（第1节）：
在介绍完函数定义后，增加：
> **哦！时刻来了**：当你定义了这个函数之后，以后任何时候想换算公里到英里，只需要写一行 `km_to_miles()`，而不是复制粘贴那四行代码。如果你想改进换算精度？只改函数定义内部，调用它的代码一行都不用动。这就是函数的威力——**改一处，处处生效**。

**效果**：让读者立即感受到函数的实际价值

#### 时刻2（第2节）：
在介绍参数后，增加：
> **哦！又来一个**：带参数之后，函数就像个"模板"——你传什么进去，它就处理什么。这比每次都写一遍同样的逻辑强一万倍。

**效果**：用"模板"这个比喻，让参数的价值更直观

#### 时刻3（第2节）：
在 `km_to_miles(user_value * 2)` 例子后，增加：
> 小北试了一下，输入 `10`，结果输出了三行：10 公里、20 公里、5 公里的换算结果。
> "等等，"小北突然瞪大眼睛，"我没再输入数字啊！它怎么知道要算 20 和 5 的？"
> **这就是函数的魔法**：你传给它什么，它就处理什么。`km_to_miles(user_value * 2)` 这行代码里，`user_value * 2` 会先算出结果（20），然后才传给函数。函数根本不知道你"怎么"得到这个 20 的——它只负责换算。

**效果**：展示函数的"意外便利"，制造惊喜感

#### 时刻4（第3节）：
在介绍返回值前，增加小北看到 `None` 的困惑：
> 小北试着运行了这段代码，结果看到一行 `None`。他以为是自己写错了，反复检查了三遍。
> "这什么意思？为什么是 None？"小北一脸困惑，"我明明看到它打印出了正确的换算结果啊！"

**效果**：用小北的真实困惑引出 print vs return 的区别

#### 时刻5（第4节）：
在作用域例子后，增加：
> **哦！意外时刻**：小北一直以为"定义了就能用"，但作用域告诉他——"在哪里定义"比"有没有定义"更重要。函数里的变量就像借来的东西，函数执行完就得还回去，外面根本拿不到。

**效果**：用"借来的东西"这个比喻，让作用域的概念更易理解

#### 时刻6（第4节）：
在全局变量例子后，增加小北的反应：
> 小北试了一下，发现 `message` 真的被改了。
> "等等，"小北突然反应过来，"那如果我有十个函数都在改 `message`，最后它变成什么鬼样子我都不知道？"
> "对！"老潘拍桌子，"所以我当年接手那个 500 行函数的项目时，光是理清'谁在什么时候改了哪个全局变量'就花了我一周。"

**效果**：用具体的灾难场景（十个函数乱改全局变量）让读者感受到问题的严重性

### 3. 小北的角色：增强画面感 ✅

#### 修改1（第1节）：
**原文**：
> "怎么回事？我明明定义了函数啊？"小北盯着屏幕，一脸茫然。

**修订**：
> "怎么回事？我明明定义了函数啊？是不是 Python 坏了？"小北盯着屏幕，反复按了五次回车，还在那儿等着。

**改进**：增加了动作描写（反复按回车、还在等），让场景更生动

#### 修改2（第1节）：
**原文**：
> 小北吃过亏后，第二次调用时又犯了一个错——他写成了这样... 运行后——什么都没发生。

**修订**：
> 小北吃过亏后，第二次调用时又犯了一个错——他写成了这样... 运行后——什么都没发生。
> "又怎么了！？"小北把手里的笔都摔了。

**改进**：增加了"摔笔"这个动作，表达小北的挫败感

#### 修改3（第2节）：
**原文**：
> 上一节的函数有个大问题：每次调用都让你输入公里数。如果你想换算 10、42、100 公里，得运行三次程序，每次输入一个数字。

**修订**：
> "这也太蠢了，"小北抱怨道，"我想把一个表里的数据全换算一遍，难道要输入一千次？"

**改进**：让小北的抱怨更具体（输入一千次），更有共鸣

#### 修改4（第3节）：
新增小北看到 `None` 时的反应（见时刻4）

### 4. 第4节作用域：更生动的比喻 ✅

**原文**：
> **局部变量**（local variable）就像房间里的东西——只有进房间才能拿到，外面看不见。
> **全局变量**（global variable）呢？就像走廊里的东西——哪里都能看到。

**修订**：
> **局部变量**（local variable）就像你手机里的私人通讯录——只有你拿着手机才能看，别人看不见。函数执行的时候，Python 会为它开辟一个"私密空间"，函数里的变量就住在这里。函数结束了，空间清空，里面的东西也就没了。
> **全局变量**（global variable）呢？就像公共广场上的广告牌——哪里都能看到。

**改进**：
- "房间里的东西" → "手机里的私人通讯录"（更具体、更现代）
- "走廊里的东西" → "公共广场上的广告牌"（更形象）
- 增加了"空间清空"的描写，让作用域的临时性更直观

### 5. 保持不变 ✅
- 核心知识点准确
- 代码可运行
- PyHelper 进度部分
- DoD、本周小结
- AI 小专栏
- Git 本周要点

## 预期效果

### 叙事流畅度
- 4/5 → 预计 5/5
- 改进：每个"哦！"时刻都自然融入叙事，不是生硬插入

### 趣味性
- 3/5 → 预计 5/5
- 改进：
  - 6个"哦！"时刻分布全章，节奏有起伏
  - 小北的错误场景更有画面感
  - 比喻更生动（手机通讯录、公共广告牌、菜谱）

### 知识覆盖
- 5/5 → 保持 5/5
- 不变：代码准确、覆盖完整

### 认知负荷
- 4/5 → 保持 4/5
- 不变：新概念数未超预算，回顾桥达标

**预计总分**：19/20（达标）

## 修订清单

- [x] 第1节开头更有冲击力
- [x] 增加6个"哦！"时刻（均匀分布在各节）
- [x] 小北角色4处改进，增强画面感
- [x] 第4节作用域比喻更生动
- [x] 保持核心知识点准确
- [x] 保持代码可运行
- [x] 保持 PyHelper 进度不变
- [x] 保持 DoD、小结不变
