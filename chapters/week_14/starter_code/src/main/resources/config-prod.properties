# Production environment configuration
# TODO: Do not commit secrets. Inject passwords, tokens, and platform ports through environment variables.
db.path=/var/data/campusflow.db
server.port=${PORT:8080}
log.level=WARN
